<!-- page title -->
<% content_for :title, "Create Organization" %>

<!-- page content -->
<div class="container-fluid container-lg">
  <div class="card">
    <div class="card-body">
      <!-- render the organization form -->
      <%= render 'organization_form' %>
    </div>
  </div>
</div>

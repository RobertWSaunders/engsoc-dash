<div class="table-responsive mh">
  <table class="table table-hover">
    <tbody>
      <% @jobs.each do |job| %>
        <%# displays all active positions as well as jobs with job postings %>
        <%= render "active_positions", :job => job %>
      <% end %>
    </tbody>
  </table>
</div>

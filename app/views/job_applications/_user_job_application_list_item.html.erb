<li class="list-group-item">

  <!-- render the dropdown for the item if configured -->
  <% if dropdown %>
    <%= render 'job_application_list_item_dropdown', application: application %>
  <% end %>

  <!-- show the job title and link to the job posting detail -->
  <h5 class="mb-sm">
    <% if application.status != "draft" %>
      <%= application.job_posting.title %>
    <% else %>
      <%= link_to edit_job_application_job_posting_answer_path(:job_application_id => application.id, :id => 1), class: "ripple" do %>
        <%= application.job_posting.title %>
      <% end %>
    <% end %>
  </h5>
  <% if application.interview.present? %>
    <p>
      <strong>Interview Scheduled for:</strong>
      <%= application.interview.start_time.strftime("%b %e, %l:%M %p") %>, to <%= application.interview.end_time.strftime("%b %e, %l:%M %p") %>
    </p>
  <% end %>


</li>

<% content_for :title, "Job Application" %>

<div class="container container-fluid">

  <div class="container-fluid container-md">
    <h5>Job Application for <%= link_to @job_posting.title, @job_posting %>, <%= link_to @job_posting.organization.name, @job_posting.organization %></h5>
    <div class="card">
      <div class="card-body">
        <%= render 'job_application_form' %>
      </div>
    </div>
  </div>
</div>
<% content_for :title, "Edit Job Posting" %>

<div class="container container-fluid">

  <div class="container-fluid container-md">

    <h5>Edit Posting for <%= @jobposting.title %></h5>
    <div class="card">
      <div class="card-body">
        <%= render 'job_posting_form' %>
      </div>
    </div>
  </div>
</div>
